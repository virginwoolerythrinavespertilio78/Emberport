<UserControl x:Class="Emberport.Views.PhpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="{StaticResource UiFontFamily}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="34,28,34,32">
        <StackPanel>

            <TextBlock Text="PHP" Style="{StaticResource PageTitleStyle}" />
            <TextBlock Style="{StaticResource PageSubtitleStyle}"
                       Text="Pick the version Apache runs with. Switching restarts Apache automatically." />

            <ItemsControl x:Name="VersionList" Margin="0,26,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0"
                                        Width="44" Height="44"
                                        CornerRadius="{StaticResource RadiusSm}"
                                        Background="#14FF6B1A"
                                        BorderBrush="#2EFF6B1A"
                                        BorderThickness="1"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="&#xE943;"
                                               FontFamily="Segoe MDL2 Assets"
                                               FontSize="18"
                                               Foreground="{StaticResource EmberBrush}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                </Border>

                                <StackPanel Grid.Column="1" Margin="16,0,16,0" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Title}"
                                                   FontSize="{StaticResource FontSizeHeading}"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}" />

                                        <Border Margin="10,0,0,0"
                                                Padding="8,2"
                                                VerticalAlignment="Center"
                                                CornerRadius="{StaticResource RadiusPill}"
                                                Background="#1F3DD68C"
                                                Visibility="{Binding ActiveBadge}">
                                            <TextBlock Text="ACTIVE"
                                                       FontSize="9"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource StatusRunningBrush}" />
                                        </Border>
                                    </StackPanel>

                                    <TextBlock Text="{Binding Location}"
                                               Margin="0,5,0,0"
                                               FontFamily="{StaticResource MonoFontFamily}"
                                               FontSize="11"
                                               TextTrimming="CharacterEllipsis"
                                               Foreground="{StaticResource TextTertiaryBrush}" />
                                </StackPanel>

                                <Button Grid.Column="2"
                                        Content="Use this version"
                                        MinWidth="140"
                                        VerticalAlignment="Center"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Visibility="{Binding SwitchAction}"
                                        Click="OnUseVersionClick" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border x:Name="EmptyState"
                    Visibility="Collapsed"
                    Margin="0,26,0,0"
                    Padding="18,16"
                    CornerRadius="{StaticResource RadiusMd}"
                    Background="{StaticResource SurfaceRaisedBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}"
                    BorderThickness="1">
                <TextBlock TextWrapping="Wrap"
                           FontSize="{StaticResource FontSizeBody}"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Text="No PHP build was found. Drop an extracted x64 Thread Safe build into the bin\php folder and restart Emberport." />
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>